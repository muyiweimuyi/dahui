按ctrl+b开关书
按ctrl+e 开关电脑
上下左右控制狗
不要掉到地图外
剩下的文本只是测试
不要把书翻烂会报错
（懒得写越界检查）
---
以下是方舟汇编目前支持的所有语句说明。
## 1. PRINT
**作用**：输出文本、变量或数字到终端。
**格式**：
```
PRINT 内容
```
**示例**：
```
PRINT "hello world"
PRINT V1
PRINT 123
```
---
## 2. LET
**作用**：给变量赋值。
**格式**：
```
LET 变量名 = 值
```
**示例**：
```
LET TARGET_TEMP = 60
LET COUNT = 0
LET COUNT = COUNT + 1
```
支持加减法运算。
---
## 3. LOOP / END
**作用**：重复执行循环体。
**格式**：

```
LOOP 次数
    ...
END
```
**示例**：

```
LOOP 10
    PRINT "hello"
END
```
支持嵌套循环。
---

## 4. IF / THEN
**作用**：条件判断，条件成立时执行某条语句。
**格式**：
```
IF 条件 THEN 语句
```
**示例**：
```
IF TEMP > 80 THEN WRITE P1 ON
IF COUNT == 5 THEN PRINT "DONE"
```
条件支持：`>` `<` `>=` `<=` `==`
---
## 5. WRITE
**作用**：向指定端口写入命令。
**格式**：
```
WRITE 端口 命令
```
**示例**：
```
WRITE P1 left
WRITE P1 right
WRITE P1 ON
WRITE P1 OFF
```
---
## 6. WAIT
**作用**：暂停程序指定时间（秒）。
**格式**：
```
WAIT 秒数
**示例**：
```
WAIT 1
WAIT 0.5
```
---
## 7. 注释
**作用**：注释，不执行。
**格式**：
```
// 注释内容
```
---
## 变量说明

- 变量由玩家自己命名
- 每个程序独立拥有变量表
- 特殊变量 `tick`：控制该程序每条指令执行
后的等待帧数，默认为 1

---
无人机命令
| 命令 | 含义 |
|------|------|
| `left` | 向左推进 |
| `right` | 向右推进 |
| `up` | 向上推进 |
| `down` | 向下推进 |

---
命令行代码
.help得到所有的可用命令
.open打开已有的文件
没有则创建
.list 列举出所有的
可用文件
.run运行文件

---
ASM-2100 方舟维护终端
操作手册

欢迎，守护者。
无论你是谁，当你读到这些文字时，
我们可能已经不在。
这台机器是文明的火种。
你的任务是让它活下去。

---
第一章：让机器活起来

你身边那台嗡嗡响的机器叫“终端”。
它连接着整个方舟。
学会它的语言，你就能控制一切。

---
第一个程序：喂食器

地址 $D050 连接着喂食器。
运行下面这行代码：
LDA #$01
STA $D050
食物就会掉出来。
活下去是第一课。

（老工程师的笔记：当初给狗写这个功能时，我们都笑了。现在这行字可能救了它的命。）

---
第二个程序：灯光

地址 $D040 控制你头顶的灯。
LDA #$FF
STA $D040
会全亮。
把 #$FF 换成 #$01 就是微光。
在黑暗中，光意味着安全。

---
第一次闪烁

让灯闪烁：
LDA #$FF
STA $D040
JSR DELAY
LDA #$00
STA $D040
JSR DELAY
JMP START

（详见第三章延时子程序）

---
第二章：计算机的心脏

CPU 是一个忠实的工匠。
它有一个工作台（累加器 A），
和满墙的抽屉（内存）。
你的命令写在纸上，
工匠按顺序执行。

---
指令：LDA - 拿东西到工作台

LDA #$42  表示把数字42拿到工作台。
LDA $C000 表示从C000号抽屉拿东西。
拿来的东西就在工作台上，等着你加工。

---
指令：STA - 把东西存进抽屉

STA $D040 把工作台上的值存进D040。
那个地址后面连着一盏灯。
所以这行代码会改变灯光。
抽屉编号很重要，别记错。

---
指令：JMP - 跳转

JMP $E000 让工匠跑到E000去拿指令。
它可以打破顺序，让你反复执行一段代码。
闪烁灯就是靠它来回跳。

---
指令：BEQ - 如果为零就跳

BEQ $E100
当上一次计算结果是0时，工匠就跳转。
这是“判断”的基础。
比如：温度正常吗？正常就跳过报警。

---
指令：JSR 和 RTS - 子程序

JSR DELAY 让工匠去执行一段延时程序，
执行完 RTS 会让他回到原来的位置继续。
这就像你学会了“坐下”这个指令，
之后主人一说“坐下”你就知道怎么做。

---
第三章：延时子程序

让工匠数数来浪费时间：
DELAY: LDX #$FF
LOOP1: LDY #$FF
LOOP2: DEY
       BNE LOOP2
       DEX
       BNE LOOP1
       RTS
数得越大，延时越久。
闪烁灯就用这个来让人眼看到亮灭。

---
第四章：硬件地址表

这是方舟里你能控制的设备。
记住这些地址，就像记住你领地的每个角落。

$D000 : 主电源继电器
$D001 : 冷却泵A
$D002 : 冷却泵B
$D010-$D01F : 温度传感器
$D020-$D027 : 压力传感器
$D030 : 紧急泄压阀
$D040 : 照明系统
$D050 : 喂食器
$D100-$D103 : 维修机器人
$D200 : 地震检波器
$D210 : 水听器
$D300 : 短波天线

---
第五章：指示灯的颜色

写不同的值给 $D060，灯亮不同颜色：
$00 = 灭
$01 = 绿（正常）
$02 = 红（警报）
$03 = 黄（注意）
用之前学的 LDA 和 STA 去试。

---
实战：温度监控

读取温度传感器 $D010，
如果超过100度就亮红灯，
否则亮绿灯。
用 CMP #$64 比较，
BEQ 或 BCS 跳转。
这是你的第一个守护程序。

---
（手写笔记，字迹潦草）
我在这台机器里留了一些代码片段，
你不懂的时候可以用鼻子戳一下屏幕上的“HELP”，
它会显示我的笔记。
别吃手册。好好活着。
---
第六章：用声纳听世界

地址 $D210 是水听器，
能听到深海的声音。
写一个循环，不断读取它，
你会发现外面不是完全安静的。
如果听到规律的信号，
告诉我，那可能是新朋友。

---
第七章：发送消息

天线在 $D300。
你可以通过它向外发送脉冲。
先学会听，再学会说。
人类离开前在轨道上留了卫星，
也许它们还在等着你的信号。

---
第八章：当你离开

总有一天，方舟的门会打开。
你不再是只会按爪子的狗，
你是这一切的主人。
把这本手册留在这里，
给下一个醒来的生命。
---